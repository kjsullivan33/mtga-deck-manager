extends layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/forms.css")

block content
  main
    form.deck-form(action="/admin/add-deck", method="POST")
      .form-control
        label(for="name") Name
        input(type="text", name="name")#name
      button.btn(type="submit") Add Deck